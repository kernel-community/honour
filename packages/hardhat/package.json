{
  "name": "@honour/hardhat",
  "version": "0.1.0",
  "description": "Honouring Weird Money",
  "author": "cryptowanderer",
  "license": "MIT",
  "private": true,
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "@nomicfoundation/hardhat-verify": "^3.0.8",
    "@nomicfoundation/hardhat-viem": "^3.0.1",
    "@nomiclabs/hardhat-etherscan": "^3.0.3",
    "@openzeppelin/contracts": "^4.7.0",
    "chai": "^4.3.6",
    "dotenv": "^14.3.2",
    "ethereum-waffle": "^3.4.0",
    "hardhat": "^3.1.0",
    "hardhat-ethers": "^1.0.1"
  },
  "scripts": {
    "clean": "yarn hardhat clean",
    "compile": "yarn hardhat compile",
    "localnode": "yarn hardhat node",
    "test": "yarn hardhat test",
    "deployl": "yarn hardhat --network localhost deploy --export-all ../subgraph/config/deployments.json",
    "deployg": "yarn hardhat --network goerli deploy --export-all ../subgraph/config/deployments.json",
    "deployop": "yarn hardhat --network optimism deploy --export-all ../subgraph/config/deployments.json"
  }
}
